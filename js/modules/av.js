// Contenu pour js/modules/av.js

function initAvPanel() {
    // Ce module n'a pas besoin de logique, 
    // l'iframe gère le simulateur de manière autonome.
    console.log("Module Simulateur AV chargé via iframe.");
}
